// No model, iniciaremos com todos os dados que a interface irá utilziar no componente
// assim sendo, um modelo de mapear todos os dados a serem usados e que terá suas mudanças

export interface ShoppingItem {
    id: string;
    name: string;
}